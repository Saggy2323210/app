import 'package:flutter/material.dart';
class GrowwSyncScreen extends StatelessWidget {
  const GrowwSyncScreen({super.key});
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Sync')), body: const Center(child: Text('Sync Placeholder')));
}
